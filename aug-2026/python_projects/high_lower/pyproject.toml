[project]
name = "high-lower"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Saurabh Kumar", email = "saurabh9.in@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "art>=6.5",
    "replit>=4.1.2",
]

[project.scripts]
high-lower = "high_lower:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
